"""Loaders for public pose-graph benchmarks, plus the held-out-constraint
cross-validation test used for E5 (no ground-truth posterior exists on real
data, so loop closures are withheld and predicted instead).
"""
